#include<bits/stdc++.h>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
    
    int t;
    
    while(t--)
    {
        
        int n,x;
        vector<int> v;
        cin>>n>>x;
        
        for(i=0;i<n;i++)
        {
            int y;
            cin>>y;
            v.push_back(y);
        }
        
        sort(v.begin(),v.end());
        
        
        
        
    }
    
    
return 0;
}